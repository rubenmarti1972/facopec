# ------------------------------------------------------------
# Strapi environment configuration for FACOPEC CMS
# Duplicate this file to .env and adjust the secrets before running
# ------------------------------------------------------------

# Server
HOST=0.0.0.0
PORT=1337
PUBLIC_URL=http://localhost:1337
APP_KEYS=test-key-1,test-key-2,test-key-3,test-key-4

# Admin panel & API security (replace all placeholders in .env)
ADMIN_JWT_SECRET=replace-with-admin-jwt-secret
API_TOKEN_SALT=replace-with-api-token-salt
TRANSFER_TOKEN_SALT=replace-with-transfer-token-salt
JWT_SECRET=replace-with-jwt-secret
SEED_ADMIN_USERNAME=facopec
SEED_ADMIN_EMAIL=facopec@facopec.org
SEED_ADMIN_PASSWORD=F4c0pec@2025

# Application URL consumed by the frontend and CORS middleware
APP_URL=http://localhost:1337
CORS_ORIGINS=http://localhost:4200

# Database configuration
# Default to SQLite for local development
DATABASE_CLIENT=sqlite

# Uncomment and complete these values to use PostgreSQL instead of SQLite
# DATABASE_CLIENT=postgres
# DATABASE_HOST=127.0.0.1
# DATABASE_PORT=5432
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SCHEMA=public
# DATABASE_SSL=false

# Upload provider
UPLOAD_PROVIDER=local
UPLOAD_MAX_SIZE=52428800

# Email provider - SendGrid (https://sendgrid.com)
# SendGrid: 100 emails/day FREE forever
#
# SETUP (5 minutes):
# 1. Sign up at https://signup.sendgrid.com/
# 2. Verify your email
# 3. Go to Settings > API Keys > Create API Key
# 4. Name: "FACOPEC Production"
# 5. Permissions: "Full Access"
# 6. Copy the API key (starts with SG....)
# 7. Paste it below
#
# IMPORTANT: SendGrid requires sender verification
# 8. Go to Settings > Sender Authentication > Verify a Single Sender
# 9. Use profeencasasedeciudaddelsur@gmail.com
# 10. Check the Gmail inbox and verify
#
SENDGRID_API_KEY=SG.your_api_key_here
EMAIL_FROM=profeencasasedeciudaddelsur@gmail.com
EMAIL_REPLY_TO=profeencasasedeciudaddelsur@gmail.com

# Misc
STRAPI_TELEMETRY_DISABLED=true
